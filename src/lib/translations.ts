// All translations are handled exclusively via the Groq API (see /api/ai/translate).
// This file is kept only for the Language type re-export used by older import paths.
export type { Language } from '@/types';
